spring:
  application:
    name: notes-app

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/notes_db?useUnicode=true&characterEncoding=UTF-8}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
        connection:
          characterEncoding: UTF-8
        useUnicode: true
    
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: true

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 100

server:
  port: 8080
  servlet:
    context-path: /
jwt:
  secret: ${JWT_SECRET:bXktc3VwZXItc2VjcmV0LWp3dC1rZXktZm9yLW5vdGVzLWFwcC1wcm9kdWN0aW9uLWVudmlyb25tZW50LWRlbW8tc2VjdXJpdHktMjU2LWJpdHM=}
  expiration: 86400000 # 24 hours in milliseconds

# Logging Configuration
logging:
  level:
    com.example.notesapp: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
